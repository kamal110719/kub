apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: myclaim-nfs
spec:
  accessModes:
  - ReadWriteMany
  volumeMode: Filesystem
  resources:
    requests:
      storage: 5Gi
  storageClassName: slow
